# 백엔드 개발 지시서
## 유관순정신계승사업회 웹사이트

> 작성일: 2026-01-17
> 기준 문서: 백엔드 개발 상세 명세서 정리.pdf
> 프론트엔드 기준: 현재 웹사이트 HTML 구조 100% 반영

---

## 1. 기술 스택

| 항목 | 기술 |
|------|------|
| Framework | Django 4.x + Django REST Framework (DRF) |
| 인증 | JWT (djangorestframework-simplejwt) |
| 미디어 저장소 | AWS S3 (운영) / 로컬 Media 폴더 (개발) |
| DB | PostgreSQL (권장) 또는 MySQL |
| Admin UI | django-jazzmin (현대적 UI) |
| 엑셀 출력 | openpyxl |
| PDF 생성 | WeasyPrint |
| 알림톡 | 비즈엠 또는 알리고 API |

---

## 2. 프로젝트 구조

```
yugwan_backend/
├── config/                 # Django 설정
│   ├── settings/
│   │   ├── base.py
│   │   ├── local.py
│   │   └── production.py
│   ├── urls.py
│   └── wsgi.py
│
├── apps/
│   ├── accounts/          # 회원 관리
│   │   ├── models.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── admin.py
│   │
│   ├── contest/           # 웅변대회
│   │   ├── models.py
│   │   ├── serializers.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   ├── admin.py
│   │   ├── signals.py     # 알림톡 발송
│   │   └── validators.py  # 파일 검증
│   │
│   ├── archive/           # 자료실
│   │   ├── models.py      # Notice, News, Gallery
│   │   ├── serializers.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── admin.py
│   │
│   ├── join/              # 후원·참여
│   │   ├── models.py      # Volunteer, Donation
│   │   ├── serializers.py
│   │   ├── views.py
│   │   ├── urls.py
│   │   └── admin.py
│   │
│   └── popup/             # 팝업 관리
│       ├── models.py
│       ├── serializers.py
│       ├── views.py
│       └── admin.py
│
├── templates/
│   └── admin/             # Admin 커스텀 템플릿
│
└── media/                 # 업로드 파일
    ├── contest/scripts/
    ├── gallery/
    ├── news/
    └── popup/
```

---

## 3. 데이터베이스 모델

### 3-1. accounts/models.py - 회원

```python
from django.contrib.auth.models import AbstractUser
from django.db import models

class User(AbstractUser):
    """
    회원 모델 (Django User 확장)
    - 프론트엔드: join/membership.html
    """
    TIER_CHOICES = [
        ('L1', '일반회원'),      # 연 30,000원
        ('L2', '후원회원'),      # 연 100,000원
        ('L3', '평생회원'),      # 500,000원
    ]

    JOIN_SOURCE_CHOICES = [
        ('search', '인터넷 검색'),
        ('sns', 'SNS'),
        ('recommend', '지인 추천'),
        ('event', '행사 참여'),
        ('news', '언론 보도'),
        ('other', '기타'),
    ]

    # 회원 등급
    tier = models.CharField(
        max_length=2,
        choices=TIER_CHOICES,
        default='L1',
        verbose_name='회원등급'
    )

    # 기본 정보
    nickname = models.CharField(max_length=50, blank=True, verbose_name='활동명')
    phone = models.CharField(max_length=11, verbose_name='연락처')  # 하이픈 제외
    address = models.TextField(blank=True, verbose_name='주소')
    birth_date = models.DateField(null=True, blank=True, verbose_name='생년월일')
    occupation = models.CharField(max_length=100, blank=True, verbose_name='직업/소속')

    # 가입 정보
    join_source = models.CharField(
        max_length=20,
        choices=JOIN_SOURCE_CHOICES,
        blank=True,
        verbose_name='가입경로'
    )
    join_message = models.TextField(blank=True, verbose_name='가입동기')

    # 후원/동의 정보
    is_donor = models.BooleanField(default=False, verbose_name='CMS후원여부')
    marketing_agreed = models.BooleanField(default=False, verbose_name='마케팅수신동의')

    # 소셜 로그인
    social_provider = models.CharField(
        max_length=20,
        blank=True,
        verbose_name='소셜로그인'
    )  # kakao / naver / google

    created_at = models.DateTimeField(auto_now_add=True)
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        verbose_name = '회원'
        verbose_name_plural = '회원 관리'
```

### 3-2. contest/models.py - 웅변대회 신청

```python
from django.db import models
from apps.contest.validators import validate_script_file

class ContestApplication(models.Model):
    """
    웅변대회 참가 신청서
    - 프론트엔드: contest/apply.html
    - 가장 데이터 무결성이 중요한 테이블
    """
    DIVISION_CHOICES = [
        ('elementary', '초등부'),
        ('middle', '중등부'),
        ('high', '고등부'),
        ('korean', '한국어 웅변'),
        ('english', '영어 웅변'),
    ]

    GRADE_CHOICES = [
        ('초4', '초등학교 4학년'),
        ('초5', '초등학교 5학년'),
        ('초6', '초등학교 6학년'),
        ('중1', '중학교 1학년'),
        ('중2', '중학교 2학년'),
        ('중3', '중학교 3학년'),
        ('고1', '고등학교 1학년'),
        ('고2', '고등학교 2학년'),
        ('고3', '고등학교 3학년'),
    ]

    STATUS_CHOICES = [
        ('SUBMITTED', '접수완료'),
        ('CHECKING', '서류검토중'),
        ('ACCEPTED', '참가확정'),
        ('REJECTED', '반려'),
    ]

    # === 대회 정보 ===
    contest_year = models.IntegerField(verbose_name='대회연도')  # 2026

    # === 참가자 정보 ===
    name = models.CharField(max_length=50, verbose_name='참가자 성명')
    birth_date = models.DateField(verbose_name='생년월일')
    school_name = models.CharField(max_length=100, verbose_name='학교명')
    grade = models.CharField(
        max_length=20,
        choices=GRADE_CHOICES,
        verbose_name='학년'
    )

    # === 참가 부문 ===
    division = models.CharField(
        max_length=20,
        choices=DIVISION_CHOICES,
        verbose_name='참가부문'
    )
    speech_title = models.CharField(max_length=200, verbose_name='웅변 제목')

    # === 연락처 정보 ===
    parent_name = models.CharField(max_length=50, verbose_name='보호자 성함')
    contact_parent = models.CharField(max_length=11, verbose_name='보호자 연락처')  # 하이픈 제외
    teacher_name = models.CharField(max_length=50, blank=True, verbose_name='지도교사')
    email = models.EmailField(verbose_name='이메일')
    address = models.CharField(max_length=200, blank=True, verbose_name='주소')

    # === 원고 파일 ===
    script_file = models.FileField(
        upload_to='contest/scripts/%Y/',
        validators=[validate_script_file],
        verbose_name='원고 파일'
    )  # .hwp, .pdf, .docx만 허용

    # === 상태 관리 ===
    status = models.CharField(
        max_length=20,
        choices=STATUS_CHOICES,
        default='SUBMITTED',
        verbose_name='처리상태'
    )
    admin_memo = models.TextField(blank=True, verbose_name='관리자 메모')
    reject_reason = models.TextField(blank=True, verbose_name='반려 사유')

    # === 동의 사항 ===
    rules_agreed = models.BooleanField(default=False, verbose_name='참가요강 동의')
    privacy_agreed = models.BooleanField(default=False, verbose_name='개인정보 동의')
    news_agreed = models.BooleanField(default=False, verbose_name='소식수신 동의')

    # === 타임스탬프 ===
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='접수일시')
    updated_at = models.DateTimeField(auto_now=True)

    class Meta:
        verbose_name = '웅변대회 신청서'
        verbose_name_plural = '웅변대회 신청서 관리'
        ordering = ['-created_at']

    def __str__(self):
        return f"[{self.contest_year}] {self.name} - {self.get_division_display()}"
```

### 3-3. contest/validators.py - 파일 검증

```python
import os
from django.core.exceptions import ValidationError

ALLOWED_EXTENSIONS = ['.hwp', '.pdf', '.docx']
BLOCKED_EXTENSIONS = ['.exe', '.sh', '.bat', '.cmd', '.ps1', '.js', '.php']
MAX_FILE_SIZE = 10 * 1024 * 1024  # 10MB

def validate_script_file(file):
    """원고 파일 검증 - 보안 필수"""
    ext = os.path.splitext(file.name)[1].lower()

    # 실행 파일 차단 (서버 해킹 방지)
    if ext in BLOCKED_EXTENSIONS:
        raise ValidationError('보안상 해당 파일 형식은 업로드할 수 없습니다.')

    # 허용 확장자 검사
    if ext not in ALLOWED_EXTENSIONS:
        raise ValidationError(
            f'hwp, pdf, docx 파일만 업로드 가능합니다. (현재: {ext})'
        )

    # 파일 크기 검사
    if file.size > MAX_FILE_SIZE:
        raise ValidationError('파일 크기는 10MB를 초과할 수 없습니다.')
```

### 3-4. archive/models.py - 자료실

```python
from django.db import models

class Notice(models.Model):
    """
    공지사항
    - 프론트엔드: archive/notice.html
    """
    CATEGORY_CHOICES = [
        ('important', '중요'),
        ('event', '행사'),
        ('contest', '웅변대회'),
        ('general', '일반'),
    ]

    category = models.CharField(
        max_length=20,
        choices=CATEGORY_CHOICES,
        verbose_name='카테고리'
    )
    title = models.CharField(max_length=200, verbose_name='제목')
    content = models.TextField(verbose_name='내용')
    author = models.CharField(max_length=50, verbose_name='작성부서')  # 사무국, 사업국 등
    views = models.IntegerField(default=0, verbose_name='조회수')
    is_pinned = models.BooleanField(default=False, verbose_name='상단고정')
    created_at = models.DateTimeField(auto_now_add=True, verbose_name='작성일')

    class Meta:
        verbose_name = '공지사항'
        verbose_name_plural = '공지사항 관리'
        ordering = ['-is_pinned', '-created_at']


class News(models.Model):
    """
    보도자료
    - 프론트엔드: archive/news.html
    """
    source = models.CharField(max_length=50, verbose_name='언론사')  # KBS, 연합뉴스 등
    title = models.CharField(max_length=200, verbose_name='제목')
    excerpt = models.TextField(verbose_name='요약')
    link_url = models.URLField(verbose_name='원문링크')
    thumbnail = models.ImageField(
        upload_to='news/thumbnails/%Y/%m/',
        blank=True,
        verbose_name='썸네일'
    )
    is_featured = models.BooleanField(default=False, verbose_name='주요보도')
    published_date = models.DateField(verbose_name='보도일')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '보도자료'
        verbose_name_plural = '보도자료 관리'
        ordering = ['-published_date']


class GalleryAlbum(models.Model):
    """
    갤러리 앨범
    - 프론트엔드: archive/gallery.html
    """
    CATEGORY_CHOICES = [
        ('contest', '웅변대회'),
        ('memorial', '추모행사'),
        ('education', '교육활동'),
        ('global', '국제교류'),
        ('meeting', '회의/행사'),
    ]

    category = models.CharField(
        max_length=20,
        choices=CATEGORY_CHOICES,
        verbose_name='카테고리'
    )
    title = models.CharField(max_length=200, verbose_name='앨범명')
    description = models.TextField(blank=True, verbose_name='설명')
    event_date = models.DateField(verbose_name='행사일')
    cover_image = models.ImageField(
        upload_to='gallery/covers/%Y/',
        blank=True,
        verbose_name='대표이미지'
    )
    is_featured = models.BooleanField(default=False, verbose_name='최신앨범표시')
    views = models.IntegerField(default=0, verbose_name='조회수')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '갤러리 앨범'
        verbose_name_plural = '갤러리 앨범 관리'
        ordering = ['-event_date']

    @property
    def image_count(self):
        return self.images.count()


class GalleryImage(models.Model):
    """갤러리 이미지"""
    album = models.ForeignKey(
        GalleryAlbum,
        on_delete=models.CASCADE,
        related_name='images'
    )
    image = models.ImageField(upload_to='gallery/images/%Y/%m/')
    caption = models.CharField(max_length=200, blank=True, verbose_name='설명')
    order = models.IntegerField(default=0, verbose_name='정렬순서')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '갤러리 이미지'
        verbose_name_plural = '갤러리 이미지'
        ordering = ['order', 'created_at']


class GalleryVideo(models.Model):
    """
    영상 갤러리
    - 프론트엔드: archive/gallery.html 하단
    """
    title = models.CharField(max_length=200, verbose_name='제목')
    youtube_url = models.URLField(verbose_name='YouTube 링크')
    thumbnail = models.ImageField(
        upload_to='gallery/videos/',
        blank=True,
        verbose_name='썸네일'
    )
    duration = models.CharField(max_length=10, verbose_name='재생시간')  # "15:32"
    views = models.IntegerField(default=0, verbose_name='조회수')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '영상'
        verbose_name_plural = '영상 갤러리 관리'
        ordering = ['-created_at']
```

### 3-5. join/models.py - 후원·참여

```python
from django.db import models

class VolunteerApplication(models.Model):
    """
    자원봉사 신청
    - 프론트엔드: join/volunteer.html
    """
    PROGRAM_CHOICES = [
        ('contest', '웅변대회 운영'),
        ('education', '역사교육 봉사'),
        ('memorial', '추모행사 지원'),
        ('office', '사무국 지원'),
    ]

    # 기본 정보
    name = models.CharField(max_length=50, verbose_name='이름')
    birth_date = models.DateField(verbose_name='생년월일')
    phone = models.CharField(max_length=11, verbose_name='연락처')
    email = models.EmailField(verbose_name='이메일')
    occupation = models.CharField(max_length=100, verbose_name='직업/소속')

    # 봉사 정보
    programs = models.JSONField(verbose_name='참여 프로그램')  # ["contest", "education"]
    available_dates = models.TextField(verbose_name='참여 가능 일정')
    experience = models.TextField(blank=True, verbose_name='관련 경험')
    motivation = models.TextField(verbose_name='지원 동기')

    # 동의
    privacy_agreed = models.BooleanField(default=False, verbose_name='개인정보동의')

    # 상태
    is_confirmed = models.BooleanField(default=False, verbose_name='승인여부')
    admin_memo = models.TextField(blank=True, verbose_name='관리자메모')

    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '자원봉사 신청'
        verbose_name_plural = '자원봉사 신청 관리'
        ordering = ['-created_at']


class Donation(models.Model):
    """
    후원 신청
    - 프론트엔드: join/donation.html
    """
    TYPE_CHOICES = [
        ('once', '일시 후원'),
        ('monthly', '정기 후원'),
    ]

    user = models.ForeignKey(
        'accounts.User',
        on_delete=models.SET_NULL,
        null=True,
        blank=True,
        verbose_name='회원'
    )
    donation_type = models.CharField(
        max_length=10,
        choices=TYPE_CHOICES,
        verbose_name='후원유형'
    )
    amount = models.IntegerField(verbose_name='후원금액')

    # 후원자 정보
    donor_name = models.CharField(max_length=50, verbose_name='후원자명')
    phone = models.CharField(max_length=11, verbose_name='연락처')
    email = models.EmailField(verbose_name='이메일')

    # 기부금 영수증
    receipt_requested = models.BooleanField(default=False, verbose_name='영수증요청')
    receipt_name = models.CharField(max_length=50, blank=True, verbose_name='영수증 발급명')
    receipt_id = models.CharField(max_length=20, blank=True, verbose_name='주민/사업자번호')

    # 상태
    is_confirmed = models.BooleanField(default=False, verbose_name='입금확인')
    confirmed_at = models.DateTimeField(null=True, blank=True, verbose_name='입금확인일시')

    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '후원'
        verbose_name_plural = '후원 관리'
        ordering = ['-created_at']
```

### 3-6. popup/models.py - 팝업 관리

```python
from django.db import models
from django.utils import timezone

class Popup(models.Model):
    """
    팝업 관리
    - 프론트엔드: index.html (메인페이지)
    """
    title = models.CharField(max_length=100, verbose_name='제목')
    pc_image = models.ImageField(upload_to='popup/', verbose_name='PC 이미지')
    mobile_image = models.ImageField(upload_to='popup/', verbose_name='모바일 이미지')
    link_url = models.URLField(blank=True, verbose_name='클릭시 이동URL')
    start_at = models.DateTimeField(verbose_name='노출 시작일시')
    end_at = models.DateTimeField(verbose_name='노출 종료일시')
    is_active = models.BooleanField(default=True, verbose_name='활성화')
    created_at = models.DateTimeField(auto_now_add=True)

    class Meta:
        verbose_name = '팝업'
        verbose_name_plural = '팝업 관리'
        ordering = ['-created_at']

    @property
    def is_visible(self):
        """현재 노출 중인지 확인"""
        now = timezone.now()
        return self.is_active and self.start_at <= now <= self.end_at
```

---

## 4. API 엔드포인트

### 4-1. URL 구조

```python
# config/urls.py
urlpatterns = [
    path('admin/', admin.site.urls),
    path('api/v1/', include([
        path('auth/', include('apps.accounts.urls')),
        path('contest/', include('apps.contest.urls')),
        path('notices/', include('apps.archive.urls.notice_urls')),
        path('news/', include('apps.archive.urls.news_urls')),
        path('gallery/', include('apps.archive.urls.gallery_urls')),
        path('volunteers/', include('apps.join.urls.volunteer_urls')),
        path('donations/', include('apps.join.urls.donation_urls')),
        path('members/', include('apps.accounts.urls')),
        path('popups/', include('apps.popup.urls')),
    ])),
]
```

### 4-2. 웅변대회 API

| Method | URL | 설명 | Request | Response |
|--------|-----|------|---------|----------|
| `POST` | `/api/v1/contest/apply/` | 참가 신청 | Form-Data | `201 {id, message}` |
| `GET` | `/api/v1/contest/my-application/` | 내 신청 조회 | JWT Token | `{status, created_at, ...}` |
| `GET` | `/api/v1/contest/winners/?year=2025` | 역대 수상자 | - | `[{name, school, award, year}]` |

**신청서 접수 Request (Form-Data)**
```
name: 홍길동
birth_date: 2012-05-15
school_name: 천안초등학교
grade: 초6
division: elementary
speech_title: 유관순 열사의 정신을 이어받아
parent_name: 홍부모
contact_parent: 01012345678
teacher_name: 김선생
email: parent@email.com
address: 충남 천안시
script_file: (파일)
rules_agreed: true
privacy_agreed: true
news_agreed: false
```

**Response**
```json
{
  "id": 1234,
  "message": "접수가 완료되었습니다.",
  "receipt_number": "2026-1234"
}
```

### 4-3. 자료실 API

| Method | URL | 설명 | 파라미터 |
|--------|-----|------|----------|
| `GET` | `/api/v1/notices/` | 공지사항 목록 | `?category=important&page=1` |
| `GET` | `/api/v1/notices/{id}/` | 공지사항 상세 | - |
| `GET` | `/api/v1/news/` | 보도자료 목록 | `?year=2026&page=1` |
| `GET` | `/api/v1/gallery/albums/` | 갤러리 앨범 | `?category=contest` |
| `GET` | `/api/v1/gallery/albums/{id}/` | 앨범 상세 (이미지 포함) | - |
| `GET` | `/api/v1/gallery/videos/` | 영상 목록 | `?page=1` |

**공지사항 목록 Response**
```json
{
  "count": 50,
  "next": "/api/v1/notices/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "category": "important",
      "category_display": "중요",
      "title": "제24회 전국 유관순 웅변대회 참가 신청 안내",
      "author": "사무국",
      "views": 1234,
      "is_pinned": true,
      "created_at": "2026-01-15T09:00:00Z"
    }
  ]
}
```

### 4-4. 후원·참여 API

| Method | URL | 설명 |
|--------|-----|------|
| `POST` | `/api/v1/members/register/` | 회원가입 신청 |
| `POST` | `/api/v1/volunteers/apply/` | 자원봉사 신청 |
| `POST` | `/api/v1/donations/` | 후원 신청 |

### 4-5. 팝업 API

| Method | URL | 설명 |
|--------|-----|------|
| `GET` | `/api/v1/popups/active/` | 현재 활성 팝업 목록 |

---

## 5. Django Admin 설정

### 5-1. 웅변대회 Admin

```python
# contest/admin.py
from django.contrib import admin
from django.http import HttpResponse
from openpyxl import Workbook
from .models import ContestApplication

@admin.register(ContestApplication)
class ContestApplicationAdmin(admin.ModelAdmin):
    list_display = [
        'id', 'name', 'school_name', 'division',
        'status', 'created_at'
    ]
    list_filter = [
        'contest_year',      # 연도별 필터
        'division',          # 부문별 필터 (초/중/고)
        'status',            # 상태별 필터 (접수/확정)
    ]
    search_fields = [
        'name',              # 참가자 이름
        'contact_parent',    # 연락처
        'school_name',       # 학교명
    ]
    list_editable = ['status']
    readonly_fields = ['created_at', 'updated_at']
    actions = ['export_to_excel', 'mark_as_accepted', 'mark_as_rejected']

    fieldsets = (
        ('대회 정보', {
            'fields': ('contest_year',)
        }),
        ('참가자 정보', {
            'fields': ('name', 'birth_date', 'school_name', 'grade')
        }),
        ('참가 부문', {
            'fields': ('division', 'speech_title', 'script_file')
        }),
        ('연락처', {
            'fields': ('parent_name', 'contact_parent', 'teacher_name', 'email', 'address')
        }),
        ('처리 상태', {
            'fields': ('status', 'admin_memo', 'reject_reason')
        }),
        ('동의 사항', {
            'fields': ('rules_agreed', 'privacy_agreed', 'news_agreed'),
            'classes': ('collapse',)
        }),
    )

    @admin.action(description='선택한 신청서 엑셀 다운로드')
    def export_to_excel(self, request, queryset):
        """
        엑셀 다운로드
        컬럼 순서: 접수번호 | 성명 | 학교 | 부문 | 연락처 | 원고파일링크
        """
        wb = Workbook()
        ws = wb.active
        ws.title = "웅변대회 신청자"

        # 헤더
        headers = ['접수번호', '성명', '학교', '부문', '연락처', '원고파일링크']
        ws.append(headers)

        # 데이터
        for app in queryset:
            ws.append([
                app.id,
                app.name,
                app.school_name,
                app.get_division_display(),
                app.contact_parent,
                request.build_absolute_uri(app.script_file.url) if app.script_file else ''
            ])

        response = HttpResponse(
            content_type='application/vnd.openxmlformats-officedocument.spreadsheetml.sheet'
        )
        response['Content-Disposition'] = f'attachment; filename=contest_applications_{queryset.first().contest_year}.xlsx'
        wb.save(response)
        return response

    @admin.action(description='선택한 신청서 참가확정 처리')
    def mark_as_accepted(self, request, queryset):
        """참가확정 → 알림톡 자동 발송됨 (Signal)"""
        queryset.update(status='ACCEPTED')
        self.message_user(request, f'{queryset.count()}건이 참가확정 처리되었습니다.')

    @admin.action(description='선택한 신청서 반려 처리')
    def mark_as_rejected(self, request, queryset):
        queryset.update(status='REJECTED')
        self.message_user(request, f'{queryset.count()}건이 반려 처리되었습니다.')
```

### 5-2. 알림톡 자동 발송 Signal

```python
# contest/signals.py
from django.db.models.signals import post_save
from django.dispatch import receiver
from .models import ContestApplication

@receiver(post_save, sender=ContestApplication)
def send_notification_on_status_change(sender, instance, **kwargs):
    """
    상태가 ACCEPTED로 변경되면 알림톡 자동 발송
    """
    if instance.status == 'ACCEPTED':
        # 비즈엠 또는 알리고 API 연동
        message = f"[유관순사업회] {instance.name} 학생의 웅변대회 참가가 확정되었습니다."
        send_kakao_alimtalk(
            phone=instance.contact_parent,
            message=message
        )


def send_kakao_alimtalk(phone, message):
    """
    카카오 알림톡 발송
    - 비즈엠 또는 알리고 API 사용
    """
    import requests

    # 예시: 알리고 API
    API_KEY = 'your-api-key'
    API_URL = 'https://kakaoapi.aligo.in/akv10/alimtalk/send/'

    payload = {
        'apikey': API_KEY,
        'userid': 'your-user-id',
        'senderkey': 'your-sender-key',
        'tpl_code': 'CONTEST_ACCEPTED',  # 템플릿 코드
        'sender': '041-564-1226',
        'receiver_1': phone,
        'subject_1': '웅변대회 참가확정',
        'message_1': message,
    }

    response = requests.post(API_URL, data=payload)
    return response.json()
```

### 5-3. 기부금 영수증 PDF 생성

```python
# accounts/admin.py
from django.contrib import admin
from django.http import HttpResponse
from weasyprint import HTML
from django.template.loader import render_to_string
from .models import User

@admin.register(User)
class UserAdmin(admin.ModelAdmin):
    list_display = ['username', 'email', 'tier', 'phone', 'is_donor', 'created_at']
    list_filter = ['tier', 'is_donor']
    search_fields = ['username', 'email', 'phone']

    change_form_template = 'admin/accounts/user/change_form.html'

    def change_view(self, request, object_id, form_url='', extra_context=None):
        extra_context = extra_context or {}
        extra_context['show_receipt_button'] = True
        return super().change_view(request, object_id, form_url, extra_context)

    def get_urls(self):
        from django.urls import path
        urls = super().get_urls()
        custom_urls = [
            path(
                '<int:user_id>/generate-receipt/',
                self.admin_site.admin_view(self.generate_receipt_pdf),
                name='generate-receipt-pdf'
            ),
        ]
        return custom_urls + urls

    def generate_receipt_pdf(self, request, user_id):
        """기부금 영수증 PDF 생성"""
        user = User.objects.get(pk=user_id)

        # 해당 회원의 후원 내역 조회
        donations = user.donation_set.filter(is_confirmed=True)
        total_amount = sum(d.amount for d in donations)

        # HTML 템플릿 렌더링
        html_string = render_to_string('admin/accounts/donation_receipt.html', {
            'user': user,
            'donations': donations,
            'total_amount': total_amount,
            'year': 2025,
        })

        # PDF 생성
        html = HTML(string=html_string)
        pdf = html.write_pdf()

        response = HttpResponse(pdf, content_type='application/pdf')
        response['Content-Disposition'] = f'attachment; filename="donation_receipt_{user.username}.pdf"'
        return response
```

### 5-4. Admin 대시보드 (django-jazzmin)

```python
# settings.py
JAZZMIN_SETTINGS = {
    "site_title": "유관순사업회 관리자",
    "site_header": "유관순정신계승사업회",
    "site_brand": "관리자 페이지",
    "welcome_sign": "유관순정신계승사업회 관리 시스템",

    "icons": {
        "accounts.User": "fas fa-users",
        "contest.ContestApplication": "fas fa-microphone",
        "archive.Notice": "fas fa-bullhorn",
        "archive.News": "fas fa-newspaper",
        "archive.GalleryAlbum": "fas fa-images",
        "join.VolunteerApplication": "fas fa-hands-helping",
        "join.Donation": "fas fa-heart",
        "popup.Popup": "fas fa-window-restore",
    },

    "custom_links": {
        "contest": [{
            "name": "오늘의 통계",
            "url": "admin:dashboard",
            "icon": "fas fa-chart-line",
        }]
    },
}

# 대시보드 위젯 (custom view 구현)
# - 오늘의 방문자 수
# - 웅변대회 D-Day
# - 읽지 않은 웅변대회 신청서 수
```

---

## 6. 보안 설정

### 6-1. CORS 설정

```python
# settings.py
CORS_ALLOWED_ORIGINS = [
    "https://yugwansun.org",
    "https://www.yugwansun.org",
]

# 개발 환경
if DEBUG:
    CORS_ALLOWED_ORIGINS += [
        "http://localhost:3000",
        "http://127.0.0.1:5500",
    ]
```

### 6-2. Rate Limiting

```python
# settings.py
REST_FRAMEWORK = {
    'DEFAULT_THROTTLE_CLASSES': [
        'rest_framework.throttling.AnonRateThrottle',
        'rest_framework.throttling.UserRateThrottle',
    ],
    'DEFAULT_THROTTLE_RATES': {
        'anon': '100/hour',
        'user': '1000/hour',
        'contest_apply': '5/minute',  # 웅변대회 신청 도배 방지
    },
    'DEFAULT_PAGINATION_CLASS': 'rest_framework.pagination.PageNumberPagination',
    'PAGE_SIZE': 10,
}
```

### 6-3. 파일 업로드 보안

```python
# settings.py

# 허용 파일 형식
ALLOWED_UPLOAD_EXTENSIONS = ['.hwp', '.pdf', '.docx', '.jpg', '.jpeg', '.png', '.gif']

# 차단 파일 형식 (실행 파일)
BLOCKED_UPLOAD_EXTENSIONS = ['.exe', '.sh', '.bat', '.cmd', '.ps1', '.js', '.php']

# 최대 업로드 크기
FILE_UPLOAD_MAX_MEMORY_SIZE = 10 * 1024 * 1024  # 10MB
DATA_UPLOAD_MAX_MEMORY_SIZE = 10 * 1024 * 1024  # 10MB
```

---

## 7. 프론트엔드 연동 가이드

### 7-1. 웅변대회 신청 폼 (contest/apply.html)

```javascript
// JavaScript 연동 예시
const form = document.getElementById('applyForm');

form.addEventListener('submit', async (e) => {
    e.preventDefault();

    const formData = new FormData(form);

    // 연락처 하이픈 제거
    const phone = formData.get('contact_parent').replace(/-/g, '');
    formData.set('contact_parent', phone);

    // 대회연도 자동 설정
    formData.set('contest_year', 2026);

    try {
        const response = await fetch('/api/v1/contest/apply/', {
            method: 'POST',
            body: formData
        });

        if (response.ok) {
            const data = await response.json();
            alert(`접수가 완료되었습니다. 접수번호: ${data.receipt_number}`);
            window.location.href = 'index.html';
        } else {
            const error = await response.json();
            alert(error.message || '접수 중 오류가 발생했습니다.');
        }
    } catch (error) {
        alert('네트워크 오류가 발생했습니다.');
    }
});
```

### 7-2. 공지사항 목록 (archive/notice.html)

```javascript
// 공지사항 로드
async function loadNotices(page = 1, category = 'all') {
    let url = `/api/v1/notices/?page=${page}`;
    if (category !== 'all') {
        url += `&category=${category}`;
    }

    const response = await fetch(url);
    const data = await response.json();

    renderNotices(data.results);
    renderPagination(data.count, page);
}

function renderNotices(notices) {
    const container = document.querySelector('.notice-list');
    container.innerHTML = notices.map(notice => `
        <div class="notice-item ${notice.is_pinned ? 'pinned' : ''}" data-category="${notice.category}">
            <span class="notice-badge ${notice.category}">${notice.category_display}</span>
            <div class="notice-content">
                <h3 class="notice-title">${notice.title}</h3>
                <div class="notice-meta">
                    <span>${notice.created_at}</span>
                    <span>${notice.author}</span>
                </div>
            </div>
            <div class="notice-views">
                <div class="notice-views-number">${notice.views.toLocaleString()}</div>
                <div class="notice-views-label">조회</div>
            </div>
        </div>
    `).join('');
}
```

### 7-3. 갤러리 (archive/gallery.html)

```javascript
// 갤러리 앨범 로드
async function loadGalleryAlbums(category = 'all') {
    let url = '/api/v1/gallery/albums/';
    if (category !== 'all') {
        url += `?category=${category}`;
    }

    const response = await fetch(url);
    const data = await response.json();

    renderGalleryItems(data.results);
}

function renderGalleryItems(albums) {
    const grid = document.querySelector('.gallery-grid');
    grid.innerHTML = albums.map(album => `
        <div class="gallery-item" data-filter="${album.category}"
             data-title="${album.title}" data-date="${album.event_date}"
             data-count="${album.image_count}">
            <div class="gallery-item-image">
                ${album.cover_image
                    ? `<img src="${album.cover_image}" alt="${album.title}">`
                    : `<i class="fas fa-image placeholder-icon"></i>`
                }
                <div class="gallery-item-overlay">
                    <i class="fas fa-search-plus"></i>
                </div>
            </div>
            <span class="gallery-item-count"><i class="fas fa-images"></i> ${album.image_count}</span>
            <div class="gallery-item-info">
                <span class="gallery-item-category">${album.category_display}</span>
                <h4 class="gallery-item-title">${album.title}</h4>
                <p class="gallery-item-date">${album.event_date}</p>
            </div>
        </div>
    `).join('');
}
```

---

## 8. 개발 우선순위 및 일정

| 순위 | 기능 | 관련 페이지 | 난이도 |
|------|------|------------|--------|
| 🔴 1 | 웅변대회 신청 API + Admin | contest/apply.html | 중 |
| 🔴 2 | 공지사항 CRUD API | archive/notice.html | 하 |
| 🔴 3 | 보도자료 목록 API | archive/news.html | 하 |
| 🔴 4 | 갤러리 앨범/이미지 API | archive/gallery.html | 중 |
| 🟡 5 | 회원가입 API | join/membership.html | 중 |
| 🟡 6 | 자원봉사 신청 API | join/volunteer.html | 하 |
| 🟡 7 | 팝업 API | index.html | 하 |
| 🟢 8 | 엑셀 다운로드 | Admin | 하 |
| �� 9 | 알림톡 연동 | Admin | 중 |
| 🟢 10 | 기부금 영수증 PDF | Admin | 중 |

---

## 9. 필수 라이브러리

```txt
# requirements.txt
Django>=4.2
djangorestframework>=3.14
djangorestframework-simplejwt>=5.3
django-cors-headers>=4.3
django-jazzmin>=2.6
Pillow>=10.0
openpyxl>=3.1
WeasyPrint>=60.0
python-dotenv>=1.0
psycopg2-binary>=2.9  # PostgreSQL
requests>=2.31  # 알림톡 API
boto3>=1.28  # AWS S3 (선택)
django-storages>=1.14  # AWS S3 (선택)
```

---

## 10. 환경 변수 (.env)

```env
# Django
SECRET_KEY=your-secret-key
DEBUG=False
ALLOWED_HOSTS=yugwansun.org,www.yugwansun.org

# Database
DB_NAME=yugwan_db
DB_USER=yugwan_user
DB_PASSWORD=your-db-password
DB_HOST=localhost
DB_PORT=5432

# AWS S3 (선택)
AWS_ACCESS_KEY_ID=your-access-key
AWS_SECRET_ACCESS_KEY=your-secret-key
AWS_STORAGE_BUCKET_NAME=yugwan-media
AWS_S3_REGION_NAME=ap-northeast-2

# 알림톡 API
ALIMTALK_API_KEY=your-api-key
ALIMTALK_USER_ID=your-user-id
ALIMTALK_SENDER_KEY=your-sender-key
```

---

**문서 끝**
